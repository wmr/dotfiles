--- powerline-prompt.zsh	2014-06-17 22:18:58.000000000 +0200
+++ powerline-prompt.zsh.patched	2014-06-17 22:16:46.000000000 +0200
@@ -2,6 +2,18 @@
 # This shell prompt config file was created by promptline.vim
 #
 
+function __is_local {
+  if [[ ${__LAST_PWD} != ${PWD} ]]; then
+    if $(is_remote 2>/dev/null); then
+      export __PWD_IS_REMOTE=1
+    else
+      unset __PWD_IS_REMOTE
+    fi
+    export __LASTPWD=${PWD}
+  fi
+  return ${__PWD_IS_REMOTE}
+}
+
 function __promptline_last_exit_code {
 
   [[ $last_exit_code -gt 0 ]] || return 1;
@@ -105,6 +117,8 @@
   printf "%s" "${2}${1}${3}"
 }
 function __promptline_git_status {
+
+  __is_local || return 1
   [[ $(git rev-parse --is-inside-work-tree 2>/dev/null) == true ]] || return 1
 
   local added_symbol="●"
